import {wp} from '@/resources/config';
import {StyleSheet} from 'react-native';

export const bulletPointTextStyles = StyleSheet.create({
  container: {flexDirection: 'row'},
  pointer: {
    paddingHorizontal: wp(4),
  },
});
